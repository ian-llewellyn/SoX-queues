Implement a proper FIFO queue
- At the moment, it is pot luck what order things are processed in on a heavily loaded system.

Detect if a new file is audio using the 'file' utility
- Currently a simple check is done against a file extension

Implement output format control from the config file
- Bit rate and format would be good
- The extension should of the output file should reflect the format
